name: Audit

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  audit:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Audit cask
        run: brew audit --cask --new --online Casks/oxideterm.rb
